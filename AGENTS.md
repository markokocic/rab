# rab-specific guidelines
- run `cargo fmt && cargo check && cargo clippy` after every code change — all three must pass clean with zero warnings
- reference pi source code in `~/src/cvstree/pi-mono` for inspiration

